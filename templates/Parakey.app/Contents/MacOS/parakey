#!/bin/bash
# Parakey app launcher — runs the Python script in the project venv.
# Uses $HOME so the same script works on any Mac.
export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
LOG="$HOME/parakey/parakey.log"
MAX_BYTES=1048576  # 1 MB
mkdir -p "$(dirname "$LOG")"
if [ -f "$LOG" ] && [ "$(stat -f%z "$LOG" 2>/dev/null || echo 0)" -gt "$MAX_BYTES" ]; then
    mv -f "$LOG" "$LOG.1"
fi
exec "$HOME/parakey/.venv/bin/python" "$HOME/parakey/parakey.py" >>"$LOG" 2>&1
